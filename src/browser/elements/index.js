// import type { App } from 'vue'
import VueActiveBrowser from './ActiveBrowser'
import VueButton from './Button'

// const components = [ ActiveBrowser ]
// const install = function (app:App) {   
//   // 全局注册所有的组件  
//   components.forEach(item => {   app.component(item.name, item)   }) 
// }
// // if (typeof window !== 'undefined' && window.Vue) {   
// //   install(window.Vue) 
// // } 
// // 导出 install （如果作为一个包，一定要有install ！！！） 

// const VueActiveBrowser = {
//   install
// }

// export { ActiveBrowser }
export {  VueActiveBrowser ,VueButton}